/** Ação de exportar no escritório: azul claro, distinto do verde da marca. */
export const EXPORT_ACTION_CLASS =
  "border-0 bg-tb-soft text-tb shadow-sm hover:bg-tb-soft/85 hover:text-tb";

/**
 * CTA no herói invertido (verde). O `default` do botão é o mesmo verde do
 * header e some; este fundo claro mantém o contraste.
 */
export const INVERTED_HERO_CTA_CLASS =
  "border-0 bg-primary-soft text-primary-strong shadow-sm hover:bg-primary-soft/85 hover:text-primary-strong";

/** Secundário no herói invertido — borda clara sobre o verde. */
export const INVERTED_HERO_SECONDARY_CLASS =
  "border-white/40 bg-white/10 text-primary-foreground hover:bg-white/20 hover:text-primary-foreground";

/** Colheita na listagem: mesmo verde de “Registrar colheita”, um tom mais claro. */
export const HARVEST_ROW_ACTION_CLASS =
  "border border-primary-border bg-primary-soft text-primary-strong shadow-sm hover:bg-primary-soft/85 hover:text-primary-strong";
